<!DOCTYPE html>
<html>
  <head>
  <title><%= yield(:title) %> | Mediflection</title>
    <%= csrf_meta_tags %>
    <%= javascript_pack_tag 'user-bundle' %>
  </head>

  <body>

    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>
    <div class="container">
        <%= yield %>
    </div>

  </body>
</html>
